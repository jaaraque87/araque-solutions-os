# HANDOFF 2026-07-07 — Camera Lab × ComfyDeploy, canal Tao, dataset Naia
_Para retomar desde cualquier PC: `git pull`, leer este archivo + `memory/MEMORY.md`. Pégale esto a Claude/Codex como contexto inicial._

## ESTADO GLOBAL (qué funciona hoy)
- **ComfyDeploy** (org araquesolutions, todo en la nube, solo navegador): máquina "LTX TODO EN UNO" en **v29** (WhatDreamsCost actualizado a `bf71104` para Director V2, + TTP_Toolset + BerniniR; parches legacy del Director v1 eliminados). Workflows: MVC (Prompt Creator V5.1 / I2V V5.2 / T2V V5.2 — **PROBADO, lipsync real**), **TAO LTX Director V2**, **TAO LTX Subtitle Cleaner I2V** (fix extend+crop 192px).
- **Camera Lab** (UI local, clonado en `C:\Users\SOPORTE2\Documents\Camera-lab` del PC principal): conectado a sesiones ComfyDeploy vía túnel (`.env` → COMFYUI_URL = url de `GET /api/session/<id>`; COMFYUI_ROOT = stub local `comfy-remote-stub`). **Director V2 remoto FUNCIONA para video sin diálogo** (2 pruebas success). Arranque: `INICIAR-CAMERA-LAB.bat` → 127.0.0.1:1234. En PC nuevo: clonar `ai2764/Camera-lab` + `py scripts/agent_setup.py` + replicar stub+.env (ver docs/camera-lab/COMPATIBILIDAD-COMFYDEPLOY.md).

## ⚠️ BUGS CONOCIDOS del modo remoto Camera Lab (Fase 2, NO resueltos)
1. **Audio NUNCA llega a la generación**: `stage_director_audio()` copia el clip al disco de COMFYUI_ROOT (asume ComfyUI local) → en remoto queda en el stub y el grafo sale SIN nodos de audio → videos mudos → **no hay lipsync posible vía Camera Lab remoto hoy**. Fix: subir audio por HTTP al ComfyUI cloud y parchear el builder (`server/camera_lab_server.py` ~línea 3713 builder ltx_director_2 / stage_director_audio ~línea 975).
2. Width/height del payload no siempre respetados (salió 704×704 pidiendo 720×1280) — mismo origen probable.
3. Las imágenes por segmento son **guías de composición, NO anclas de identidad**: con varias guías distintas el rostro se promedia a una mujer genérica. Identidad real = LoRA (pendiente) o Global Ref (video 08 del canal, sin probar).
**Conclusión operativa: talking-heads con personaje → SIEMPRE por MVC. Camera Lab remoto → B-roll/tomas cinemáticas sin diálogo.**

## PRÓXIMA ACCIÓN ACORDADA: LoRA Z-Image de NAIA (Opción B)
- Dataset CURADO: `Downloads\NAIA CRUZ\seleccion_lora\` (36 imgs, 73 MB, todos los ángulos, ver SELECCION.md) — **está solo en el PC principal: llevarlo por USB/Drive o subirlo a Assets de ComfyDeploy desde el navegador** (hay que subirlo a la nube igualmente para entrenar).
- Receta: `zimage_dataset_creator` (crop 9:16, captions describen outfit/lugar/pose, NUNCA cara/pelo/ojos; trigger `naiacruz`) → `Z-ImageTurbo_SpeedLoraTrainer_V1` **~6000 steps** (dato Discord: 3000 no basta) → LoRA al volumen `loras/` → activar en UI del I2V (Z-Image LoRA + trigger). Sesión L40S ~2 h (~$2-4).
- Después: re-render del video "Naia se presenta" por MVC con el LoRA → identidad exacta + lipsync real. Guion validado y audio YA PAGADO: `Camera-lab\tasks\camera_lab_uploads\audio\naia_presentacion.mp3` (15.84s, ElevenLabs voz rzpLrJDiI1CBeAvkbjNf) — llevarlo también o regenerar (guion en docs/camera-lab/ y en el chat 7 jul).

## OTROS FRENTES
- **Canal Tao of AI**: 12/12 videos documentados en `docs/taoofai/` + `docs/camera-lab/video-01-*.md`. Herramienta reusable: `tools/yt-analyze/analyze_channel.py` (Gemini video→md, GRATIS). 
- **omni-edit**: skill v2 + ejecutor en `tools/omni-flash/` listos; bloqueado por billing Google (free tier video=0). Piloto armado (`pilot_smoke.json`). Ver `memory/project_omni_edit.md`.
- **Biblioteca Higgsfield**: 98 skills en `docs/higgsfield-skill-library/`, 9 activas con prefijo `hf-`.
- **Fase 2 Camera Lab**: modelos Bernini-R GGUF + WAN VACE + SCAIL al volumen (matriz en docs/camera-lab/COMPATIBILIDAD-COMFYDEPLOY.md) + fix staging remoto.
- **Plataforma ARAQUE**: patrón validado = API server local/nube + workflows dedicados + sesiones ComfyDeploy por API; diseño en COMPATIBILIDAD-COMFYDEPLOY.md §Integración.

## SECRETOS (NUNCA por git)
`.env` del repo está gitignoreado. En el PC nuevo copiar a mano: GEMINI_API_KEY (válida, formato AQ.A...), ELEVENLABS key, HEYGEN. Regla del dueño: no gastar créditos de API sin su OK explícito.
